# 安全策略

## 支持的版本

安全修复只针对以下版本：

| 版本 | 支持状态 |
| :--- | :--- |
| `master` 分支最新提交 | 支持 |
| Releases 中 `easysb` 标签的最新脚本 | 支持 |
| 更早的脚本版本 | 不支持，请先升级 |

## 报告安全问题

**请不要通过公开 Issue 报告安全漏洞。**

请通过以下方式私下报告：

- 发送邮件至 zj18139624826@gmail.com，标题以 `[EasySB Security]` 开头。
- 或在仓库的 Security 页面使用 GitHub 私密漏洞报告功能。

报告中请尽量包含：

- 问题类型与影响范围，例如命令注入、越权、敏感信息泄露。
- 受影响的脚本版本、系统与架构。
- 完整的复现步骤，或最小可复现示例。
- 你建议的修复方向（可选）。

## 响应流程

- 收到报告后会在 3 个工作日内确认。
- 确认问题后会评估影响范围，并给出修复计划与时间预期。
- 修复发布后会在 [CHANGELOG.md](CHANGELOG.md) 中说明，并视情况致谢报告者。

## 范围说明

以下内容属于预期行为，不作为安全漏洞处理：

- `templates/vless-vision-reality/` 等模板目录中的 UUID、密码、私钥与域名是**示例值**，不是泄露。请在使用前自行生成。
- 部署脚本需要 root 权限执行系统级操作，这属于设计目标。
- 脚本中的 GitHub 反代加速地址是可替换的第三方服务，不保证可用性。

以下内容不在受理范围：

- 面向第三方服务器的攻击脚本、扫描器或漏洞利用代码。
- 要求绕过目标平台的鉴权、风控或服务条款的改动。

## 安全使用建议

- 部署前重新生成全部密钥与 UUID，服务端与客户端保持一致。
- REALITY 私钥仅保留在服务端，不要提交到任何公开仓库。
- 证书与私钥文件权限收紧至 `600`。
- 订阅地址包含 token，请勿公开分享，泄露后及时在脚本中重置。
- 仅在合法授权的网络环境中使用本项目。
